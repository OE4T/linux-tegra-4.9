* Nvidia Tegra Micro Coded Engine (MCE)

Required properties:
- compatible: The driver is compatible with
  "nvidia,tegra186-mce"

- reg: base address and size of MCE aperture

Example:

mce {
	compatible = "nvidia,tegra186-mce";
	reg = <0x0 0x0e000000 0x0 0x60000>;
};
